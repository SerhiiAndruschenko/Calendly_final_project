export const usersList = (state) => state.user.users;
export const currentUser = (state) => state.user.user;
export const currentToken = (state) => state.user.token;
export const eventsList = (state) => state.events.events;
export const isLoading = (state) => state.events.isLoading;
